"""Generators for the article support pack.

`make_tables` renders `TABLES.md` from a committed benchmark run and
`make_charts` draws the PNGs in `charts/`. Both are scripts rather than library
code: they exist so that no figure in an article about this project is ever
transcribed by hand.
"""
