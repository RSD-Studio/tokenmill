# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Editors / OS
.idea/
.vscode/
.DS_Store

# Model and tokenizer caches pulled at runtime by optional backends
.cache/
models/

# Benchmark scratch output; committed results live in benchmarks/results/
benchmarks/scratch/

# The sample_repo fixture is a real git repo. Its working files are
# committed; its .git directory is recreated on demand by
# scripts/make_fixtures.py so a clone gets the files, not an empty gitlink.
tests/fixtures/sample_repo/.git/

# Hypothesis keeps a local database of falsifying examples.
.hypothesis/
